import React, { Component } from 'react';

export default class SearchInput extends Component {
  static propTypes = {

  };

  render() {
    return (
      <div className="common-search-input">
        Component content: common/SearchInput
      </div>
    );
  }
}
